# Optional: use backend for review (set this and have frontend call POST /api/review)
# Get your API key from https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# Optional: set to skip taking screenshots (e.g. if Puppeteer is not installed)
# SKIP_SCREENSHOT=false

# Optional: server port (default 3001)
# PORT=3001
 
# Optional: override the default GROQ model. See deprecations doc for supported models:
# https://console.groq.com/docs/deprecations
# GROQ_MODEL=your_supported_model_name
